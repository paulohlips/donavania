import styled from 'styled-components';

export const ButtonContainer = styled.form`
    display: flex;
    flex-direction: row;
    margin: 5px;

    input[type='radio'] {
        transform: scale(1.1);
        margin-top: 2px;
    }

    p {
        margin-left: 10px;
    }
`;
